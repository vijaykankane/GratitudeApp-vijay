apiVersion: v1
kind: Service
metadata:
  name: moods-service-cluster-ip-service
  namespace: vijay-gratitude
spec:
  type: ClusterIP
  selector:
    component: moods-service
  ports:
    - port: 50052
      targetPort: 50052
